# 9. Write a R program to create an 3 dimensional array
# of 30 elements using the dim() function.

# Create a vector with 30 elements
elements <- 1:30

# Define dimensions (e.g., 3x5x2)
dim(elements) <- c(3, 5, 2)

# Print the 3D array
print(elements)
