vector1 <- c(1, 2, 3)

vector2 <- c(4, 5, 6)

# Combining as columns
combined_columns <- cbind(vector1, vector2)

# Combining as rows
combined_rows <- rbind(vector1, vector2)

print(combined_columns)
print("\n")
print(combined_rows)
